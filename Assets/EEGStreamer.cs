using System.Collections;
using System.Collections.Generic;
using UnityEngine;

/*
 * A tool for taking EEG data from a headset, matching to local clock time, (saving), and passing to python for processing
 * */

public class EEGStreamer : MonoBehaviour
{
    public string headsetName;

    // Start is called before the first frame update
    void Start()
    {
        // init inlet stream byprop

        // init outlet stream byprop
    }

    // Update is called once per frame
    void Update()
    {
        // pull chunk

        // write to file 

        // push chunk
    }
}
